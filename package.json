{
  "name": "url-shortener",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "database",
    "backend",
    "frontend"
  ],
  "scripts": {
    "dev:backend": "npm run dev --workspace=backend",
    "dev:frontend": "npm run dev --workspace=frontend",
    "lint": "eslint ."
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1"
  }
}
